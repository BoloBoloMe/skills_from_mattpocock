# 重构候选项

TDD 循环之后，寻找：

- **重复** → 提取函数/类
- **长方法** → 拆成私有辅助函数（helper，测试仍保持在公共接口上）
- **浅模块** → 合并或加深
- **依恋情结（Feature envy）** → 将逻辑移动到数据所在的位置
- **基本类型偏执（Primitive obsession）** → 引入值对象（value objects）
- 新代码暴露出有问题的**现有代码**
